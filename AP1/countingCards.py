import time

while True:
    # Capture screen
    # Preprocess image
    # Detect and read cards
    # Update Hi-Lo count
    # Advise on betting
    # Wait for a short period before the next iteration
    time.sleep(1)
    